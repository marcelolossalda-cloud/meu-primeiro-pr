# site/

Conteúdo publicado em `marceloemilene.com`.

- `index.html` — **arquivo gerado**. Não edite aqui.
- `CNAME` — domínio usado pelo GitHub Pages.

A página é escrita em `../pagina-de-vendas/index.html`. Depois de editar,
rode `python3 pagina-de-vendas/build.py` para regerar este `index.html`.
